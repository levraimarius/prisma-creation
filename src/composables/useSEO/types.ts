export interface SEOMetadata {
  title: string;
  description: string;
  image?: string;
  url?: string;
  region: string;
  city: string;
  coordinates: string;
  latitude: number;
  longitude: number;
}
